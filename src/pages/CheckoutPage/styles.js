export const styles = {
    wrapper: {
        padding: '24px', // theme.spacing(3) * 8px
        backgroundColor: '#f5f5f5', // theme.palette.background.default
    },
    gridContainer: {
        margin: 0,
        padding: 0,
    },
    cartDetails: {
        padding: '16px', // theme.spacing(2) * 8px
        borderRadius: '8px', // theme.shape.borderRadius
        boxShadow: '0px 4px 6px rgba(0, 0, 0, 0.1)', // theme.shadows[2]
    },
    cartHeader: {
        display: 'flex',
        justifyContent: 'space-between',
        paddingBottom: '8px', // theme.spacing(1) * 8px
        borderBottom: '1px solid #e0e0e0', // theme.palette.divider
    },
    cartHeaderText: {
        fontWeight: 600, // theme.typography.fontWeightBold
    },
    cartItem: {
        display: 'flex',
        justifyContent: 'space-between',
        padding: '8px 0', // theme.spacing(1, 0)
        borderBottom: '1px solid #e0e0e0', // theme.palette.divider
    },
    cartItemText: {
        flex: 1,
        textAlign: 'center',
    },
    summary: {
        padding: '16px', // theme.spacing(2) * 8px
        borderRadius: '8px', // theme.shape.borderRadius
        boxShadow: '0px 4px 6px rgba(0, 0, 0, 0.1)', // theme.shadows[2]
    },
    summaryItem: {
        display: 'flex',
        justifyContent: 'space-between',
        paddingBottom: '8px', // theme.spacing(1) * 8px
        borderBottom: '1px solid #e0e0e0', // theme.palette.divider
    },
    checkoutButtonWrapper: {
        marginTop: '16px', // theme.spacing(2) * 8px
    },
};
